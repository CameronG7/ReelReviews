{{!-- <div class="row">
  <div class="col-auto">
    <h2>Let's see what you've had to say</h2>
  </div>
</div> --}}


<ul class="mt-3" id="commentList">
  <h1>Welcome back {{user.username}}!</h1>
  <h3>Here are some of the reviews you have given:</h3>
   <button class="text-light display-4" href="/dashboard" id="dashBoardBtn">Return to Dashboard</button>
  {{#each user.reviews as |review|}}
    <li>{{review.comment}}</li>
    <li>{{review.rating}}</li>
  {{/each}}
</ul>

  <div class="col-md-4">
          <button class="btn btn-sm btn-danger" id="updateBtn" data-id="{{review.id}}">UPDATE</button>
        </div>

<li class="nav-item">
<a class="nav-link active text-dark" aria-current="page" id="deleteUserBtn" href="/deleteUser">💀👤</a>
</li>  

<section id='newreview' class='card text-center'>
  <div class='card-body'>
    <h3 class='card-title'>Create a New Review:</h3>
    <form class='form new-review-form'>
      <div class='form-group'>
        <label for='review-title'>Movie Title:</label>
        <div></div>
        <input
          class='form-input'
          type='text'
          id='review-title'
          name='review-title'
        />
      </div>
      <div class='form-group mt-2'>
        <label for='review-body clas'>Tell us what you thought:</label>
        <div></div>
        <input
          class='form-input'
          id='review-body'
          name='review-body'
          />
      </div>
      <div class='form-group mt-2'>
        <button type='submit' class='btn btn-primary' id="createBtn">Create</button>
      </div>
    </form>
  </div>
</section>  